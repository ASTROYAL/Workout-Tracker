## 2024-05-18 - Added Tooltips to IconButtons
**Learning:** Flutter `IconButton`s without tooltips omit critical accessibility labels for screen readers. Providing a tooltip adds both visual hover/long-press text and proper screen reader semantics.
**Action:** Always add the `tooltip` property to `IconButton` widgets to ensure they are accessible and intuitive.
